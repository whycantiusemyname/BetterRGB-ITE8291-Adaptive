# Better RGB 中文增强版 v3.3.0

适用于机械革命苍龙 16 X Pro 的 ITE8291 单键 RGB 键盘控制器。程序不会刷写 BIOS、EC 或键盘固件。

## 新增功能

- 统一电源过渡：启动、退出、无操作休眠和输入唤醒共用同一套动画，可选择全局渐变、中心/四边/上下左右涟漪，以及两条对角蛇沿螺旋轨迹同步爬行的双蛇漩涡。
- 灯效内容保持不变：过渡只对主灯效与顶行 Bar 的最终合成帧应用物理键位遮罩，不会改写流沙、电流、同步辨色等效果本身。
- 自定义过渡时长：通过滑杆在 0.2 至 3.0 秒之间以 0.1 秒为步进调整；退出与休眠使用所选点亮动画的逆向过程。
- 按需显示细节：先选择关闭、渐变、涟漪或双蛇漩涡，仅在所选类型需要时显示方向和路径设置，不再把所有组合挤在一个下拉框中。
- J/K 中心：中心涟漪与双蛇汇合点明确锚定在字母 `J`、`K` 之间，而不是包含数字键盘后的整机几何中心。
- 双蛇螺旋：两条蛇从主对角或副对角同时沿双臂螺旋爬行，可选择外向内或严格沿同一路径反向的内向外。
- 主灯效手动过渡：点击“停止主灯效”会逆向熄灭主键区，从空白重新选择灯效会正向点亮；普通灯效之间切换不会强制经过黑屏，顶行 Bar 可持续显示。
- 横向分页界面：窗口改为紧凑横向布局，按逻辑拆分为“灯效”“插电方案”“电池方案”“顶行 Bar”“程序设置”五页，不再把所有控件纵向堆叠。
- 独立电源编辑：插电与电池页面可直接编辑各自的主灯效、亮度和目标 FPS；当前电源对应的方案会即时应用，另一套只保存、不互相覆盖。
- 独立 Bar 页面：插电 Bar 与电池 Bar 在同一页左右并排，信息类型、方向、指标双色和音频三色 RGB 一目了然。
- 顶行信息灯条：`Esc`、`F1` 至 `F12` 及右侧功能键可以独立于主键区灯效工作，用灯条长度显示电池电量、系统音量、麦克风输入、CPU/GPU 占用率或播放音频波形。
- 四向平滑显示：顶行灯条支持从左到右、从右到左、从两边到中间、从中间到两边；边界采用连续覆盖率，避免数值变化时整颗按键跳变。
- 电量色彩语义：电量条内所有已亮按键保持同一种颜色，低电量使用“低值色”（默认红色），随电量增加连续过渡到“高值色”（默认绿色）。
- 统一指标颜色：系统音量、麦克风、CPU 和 GPU 也改为整段同色，灯条长度表示数值，颜色随长度由颜色 A（默认绿）连续过渡到颜色 B（默认红）；电量为保护语义采用相反顺序，即低电量 B、高电量 A。
- 音频 RGB：播放音频波形使用三种可编辑颜色组成线性光闭环渐变，并沿所选方向连续流动，不再局限于高/低两种指标颜色。
- 插电/电池独立顶行方案：两种电源状态分别保存顶行开关、信息类型、方向和起止渐变色。顶行方案始终随插拔电源切换，不依赖主灯效的自动切换开关。
- 同步辨色：把主屏幕按键盘物理位置降采样为第二块模糊屏幕，使用线性光空间模糊和帧间平滑减少闪烁；捕获画面只存在内存中，不写入文件。
- 同步辨色使用物理桌面分辨率并启用 Per-Monitor V2 DPI 感知，避免 Windows 显示缩放导致画面右侧缺失；顶行 `Esc` 到 `End` 分别对应桌面最左到最右。
- 插电与电池双方案：两种电源状态可以分别保存灯效、亮度和目标 FPS，并在插拔电源后自动切换。
- 随机多色呼吸：每次呼吸到最低亮度时更换一种鲜艳颜色，不产生明显跳色。
- 触碰涟漪：按键作为圆心向周围扩散，可选固定颜色或每次按键随机颜色。
- 全彩渐变：整个键盘保持常亮并连续平滑循环 RGB 色彩，没有呼吸灯的变暗间隔，可调节变色速度。
- 双格彩虹：按奇偶把键盘分成两个互相镶嵌的网格，每格各自保持单色，整体沿彩虹连续循环；两格光谱方向可选反向、同向或相差 180 度互补，并可调速度、网格花样（棋盘/横向/纵向）和两格色相偏移。
- 波浪网格：沿用两个镶嵌网格，但每格沿波浪方向形成流动的彩虹波带，颜色由键位投影决定，两格再叠加相位差交织错开；可调速度、波长、波浪角度、网格花样和两格相位差。
- 随机彩波：每个键独立随机取色，变化方式可选平滑过渡、硬切换或色相漂移，并可调随机速度与过渡速度；可再叠加一层只调制亮度、不改颜色的波浪（可选开关、速度、波长、角度和强度）。
- 流沙：参考 Armoury Crate 的纵向流沙运动规律，以三种自选颜色形成首尾闭环的连续 RGB 沙带；可调速度和沙带宽度。
- 电流：每个物理键行中的短光段交错反向推进，采用横向 8 点覆盖采样和帧间余辉插值，让光头、拖尾及跨键移动连续衰减；可调三色渐变、速度和长度。
- 触控电流：按下任意键后，只在该键所在物理行向左右扩散电流；方向键按独立方向键行处理，可调三色渐变、速度和宽度。
- Enter/反斜杠底层灯位校正：反斜杠独立使用小键盘 789 行的 LED 槽，宽 Enter 使用小键盘 456 行的两个 LED 槽；所有灯效共享正确映射。
- 字符行校正：方向上键 `↑` 与小键盘 `0` 同行；方向左、下、右键保持为最下面的独立方向键行。
- 连续混色抗锯齿：上述三种新效果在每颗键面内进行 2×2 子采样，并在线性光空间平均，减少离散色带和光锋跳格。
- 近板载启动：登录任务使用更高优先级，首次启动失败时自动重试，尽可能缩短 Windows 登录后原厂灯效继续显示的时间。
- 平滑关机：程序在普通应用的最后关闭阶段退出，并在关机、注销或手动退出前逆向播放所选点亮过渡。
- 无操作熄灯：可选择关闭、1、5、10 或 30 分钟；超时后按所选过渡休眠，检测到键盘或鼠标输入后用同一过渡唤醒。
- 原厂亮度快捷键：`Fn+F6` 降低键盘灯亮度，`Fn+F7` 提高键盘灯亮度，按 0%、25%、50%、75%、100% 五档切换。

## Fn 快捷键

- `Fn+F6` 与 `Fn+F7` 通过电脑原有的 ACPI/WMI 热键事件接入，不安装新驱动、不修改 EC，也不使用全局键盘钩子。
- 快捷键调整会同步界面滑杆，并保存到当前插电或电池方案。
- `Fn+Space` 暂不支持：本机实测该组合不会向 Windows 的官方 ACPI/WMI 热键通道上报事件。为避免普通空格误触、键盘钩子安全风险和 EC 轮询开销，本版本不强行实现模式轮切。

## 电源方案

1. 在“插电方案”和“电池方案”中分别选择灯效。
2. 勾选“按插电状态自动切换灯效”。
3. 当前亮度和 FPS 会记入当前电源方案；要调整另一套亮度和 FPS，请切换到对应电源状态后再调整。
4. 从旧版升级时，自动切换默认关闭，避免首次启动就改变原灯效。

顶行信息灯条有单独的“插电信息”和“电池信息”选项，两者都可设为“关闭”。方向、高/低指标颜色和三种音频 RGB 颜色也分别保存；这套顶行切换始终生效，因此可以只开插电顶行、只开电池顶行，或让两种状态显示不同指标。

## 顶行信息灯条

- “系统音量”只读取 Windows 当前主音量，不会替用户修改音量。
- “麦克风输入”和“播放音频波形”读取默认音频端点的实时峰值，并使用快起慢落的平滑响应。
- CPU 使用率来自 Windows 系统时间差值；GPU 使用率来自 Windows 的 `GPU Engine` 性能计数器。没有可用 GPU 计数器时灯条会安全显示为零。
- 点击“停止主灯效”只停止主键区动画；顶行信息若已开启会继续刷新。选择对应顶行方案中的“关闭”才会关闭该信息灯条。

建议电池方案使用静态或呼吸效果、15 至 20 FPS 和较低亮度。RGB 灯本身的功耗有限，降低亮度通常比单纯降低 FPS 更有效。

## 安全与恢复

- 程序必须独占 RGB HID 接口；v2 已彻底禁用共享模式，无法取得独占权时会安全退出。
- 程序运行期间，官方控制中心的灯光页面不可用属于正常现象。
- 退出程序并重启官方控制中心后即可恢复原厂灯光控制。
- 若键盘灯对所有软件都无响应，请彻底关机、拔下充电器和 USB 设备，按住电源键约 30 秒，再接电开机。

## 板载模式限制

- 本程序的动态效果由 Windows 实时计算并通过 USB HID 发送，不能在 Windows 尚未登录或程序未运行时自行播放。
- 真正板载需要键盘固件或 EC 提供公开、可验证的持久化接口；本机当前协议没有发现这种安全接口，因此本版本不会刷写 BIOS、EC 或键盘固件。
- 开机到登录之间仍可能短暂显示固件保存的原厂效果；v2.3 优化的是登录后的接管速度和关机前的视觉过渡。

## 开源说明

本版本基于 EpicYamo 的 `Better-RGB-for-Tongfang_ITE8291` 修改，依据 MIT License 分发。触碰涟漪参考了 QMK 与 OpenRGB Effects 中常见的按键扩散交互形式，具体实现为本项目独立编写，没有复制不兼容许可证的源代码。
